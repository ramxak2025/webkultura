{
  "version": 2,
  "buildCommand": "npx prisma generate --schema=../../packages/database/prisma/schema.prisma && npx nest build",
  "builds": [
    {
      "src": "dist/serverless.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/serverless.js"
    }
  ]
}
