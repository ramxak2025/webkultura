{
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npx prisma generate --schema=packages/database/prisma/schema.prisma && cd apps/api && npx nest build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
